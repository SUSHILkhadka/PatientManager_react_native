import {StyleSheet} from 'react-native';

const styleImage = StyleSheet.create({
  avatar: {
    width: 200,
    height: 200,
    display:"flex",
    alignSelf: "center"

  },
});
export default styleImage;
